server:
  port: ${PORT:8762}

## Classic Eureka configuration
eureka:
  instance:
    preferIpAddress: false
    hostname: ${RAILWAY_PRIVATE_DOMAIN:localhost}
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}


## Enable service discovery (with Eureka)
## lower-case-service-id is mandatory, otherwise is retrieved in upper case from Eureka
spring:
  application:
    name: ${RAILWAY_PRIVATE_DOMAIN:gateway}
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true
          default-filters:
            - DedupeResponseHeader=Access-Control-Allow-Credentials Access-Control-Allow-Origin
          globalcors:
            cors-configurations:
              '[/**]': ## For all routes
                allowedOriginPatterns: ${ALLOWED_ORIGINS:*} # With '*' we allow all origins. We can restrict by indicating domains or ip + port, e.g. http://localhost:3000 (where our front is, or a Vercel domain)
                allowedHeaders: "*" ## All headers allowed
                allowCredentials: true
                allowedMethods:
                  - POST
                  - GET
                  - OPTIONS


## We enable the Actuator API to be able to access <<host>>/actuator/gateway/routes
## We expose the routings that have been automatically detected (all)
## Depending on the ROUTE_TABLES_ENABLED environment variable (true by default)
management:
  endpoint:
    gateway:
      access: ${ROUTE_TABLES_ENABLED:READ_ONLY}
  endpoints:
    web:
      exposure:
        include:
          - '*'

microservices:
  users:
    url: ${USERS_URL:lb://users/api/v1}